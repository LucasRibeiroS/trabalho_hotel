#ifndef SEEDS_H
#define SEEDS_H

/* 
* Creates an admin user with pre-derfined
* username and password
*/
void createAdminUser(int storageType);

/* 
* Creates categories for accommodation table
*/
void createAccommodationCategories(int storageType);

#endif